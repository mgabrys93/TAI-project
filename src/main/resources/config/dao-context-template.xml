<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!--Activates annotation configuration of beans -->
    <context:annotation-config />

    <!--Scans dao package in search for annotation configuration -->
    <context:component-scan base-package="pl.edu.agh.ki.tai.dao"/>

    <!-- jdbc dataSource used for database connection -->
    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"
          destroy-method="close">
        <property name="driverClassName" value="com.mysql.jdbc.Driver"></property>
        <property name="url" value="jdbc:mysql://localhost:3306/TAI_project"></property>
        <property name="password" value="ala123"></property>
        <property name="username" value="root"></property>
    </bean>

    <!--Transaction manager used by hibernate -->
    <bean id="transactionManager"
          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource">
        </property>
    </bean>
    <!--Searches for @Transactional and similar annotations -->
    <tx:annotation-driven transaction-manager="transactionManager"></tx:annotation-driven>

    <!--Hibernate sessionFactory -->
    <bean id="sessionFactory" name="sessionFactory"
          class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource">
        </property>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.MySQL5Dialect</prop>
                <!--Creates database automatically -->
                <prop key="hibernate.hbm2ddl.auto">update</prop>
            </props>
        </property>
        <!--Listof hibernate annotated Classes -->
        <property name="annotatedClasses">
            <list>
                <value>pl.edu.agh.ki.tai.model.User</value>
	    		<value>pl.edu.agh.ki.tai.model.Group</value>
	    		<value>pl.edu.agh.ki.tai.model.Event</value>
	    		<value>pl.edu.agh.ki.tai.model.Comment</value>
	    		<value>pl.edu.agh.ki.tai.model.UserRole</value>
            </list>
        </property>
        <!--property name="packagesToScan"> <list> <value> dao </value> </list>
            </property -->
    </bean>

    <!--Translates exceptions on all classes with @Repository from Hibernate
        to Spring exceptions -->
    <bean id="exceptionTranslator"
          class="org.springframework.dao.annotation.PersistenceExceptionTranslationPostProcessor">
    </bean>

</beans>